version: '3.8'

services:
  # CPU-only deployment (default)
  parakeet-cpu:
    build:
      context: .
      dockerfile: Dockerfile.cpu
    image: ghcr.io/0dragosh/parakeet-tdt-0.6b-v3-fastapi-openai:cpu
    container_name: parakeet-cpu
    ports:
      - "5092:5092"
    volumes:
      - parakeet-models:/app/models
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5092/health')" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # GPU deployment (requires NVIDIA Container Toolkit)
  parakeet-gpu:
    build:
      context: .
      dockerfile: Dockerfile.gpu
    image: ghcr.io/0dragosh/parakeet-tdt-0.6b-v3-fastapi-openai:latest
    container_name: parakeet-gpu
    ports:
      - "5092:5092"
    volumes:
      - parakeet-models:/app/models
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]
    healthcheck:
      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5092/health')" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

  # GPU deployment with TensorRT acceleration
  parakeet-tensorrt:
    build:
      context: .
      dockerfile: Dockerfile.gpu
    image: ghcr.io/0dragosh/parakeet-tdt-0.6b-v3-fastapi-openai:latest
    container_name: parakeet-tensorrt
    ports:
      - "5092:5092"
    volumes:
      - parakeet-models:/app/models
    environment:
      - PARAKEET_DEVICE=tensorrt
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]
    healthcheck:
      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5092/health')" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

volumes:
  parakeet-models:
    name: parakeet-models
